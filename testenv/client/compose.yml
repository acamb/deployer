services:
  test:
    image: test
    container_name: test
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      TEST_SECRET_1: "${TEST_SECRET_1}"
      TEST_SECRET_2: "${TEST_SECRET_2}"
